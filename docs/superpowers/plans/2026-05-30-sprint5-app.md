# Sprint 5 — App site redesign landing (Part B) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconcile the afterwords-app GitHub Pages site (`docs/index.html`) to the 29-May Surface 3 mockup in place, keeping all live facts (counts, release/repo URLs) authoritative over the mockup.

**Architecture:** In-place visual/layout/copy reconciliation (Approach A). Static single HTML file — no build step, no JS app logic to preserve, so risk is low. The mockup is the visual authority; live data wins on any factual conflict.

**Tech Stack:** Static HTML/CSS served via GitHub Pages from `docs/`.

**Spec:** `~/repos/afterwords/docs/superpowers/specs/2026-05-30-sprint5-redesign-landing-design.md`
**Mockup (read-only reference):** `~/repos/afterwords/afterwords-redesign/Surface 3 - afterwords app.html`

**Known facts (from QA):**
- Live `docs/index.html` (~418 ln) has three sections (control-panel / status-voices-settings / what-you-need) and an "afterwords for Mac" hero. The mockup (~331 ln) has two sections (voice-switching-from-menubar / what-you-need) and an "Afterwords" hero. Real structural delta to reconcile.
- Live release/repo URLs are correct (`github.com/adrianwedd/afterwords-app/releases`, `…/afterwords-app`) — keep them.
- **C-5 (noted, no action):** the app file re-declares palette tokens with shorthand names (`--bg`, `--fg-m`, `--acc`) vs `Base.astro`'s `--color-*`. Cosmetic; leave as-is.

---

### Task 0: Branch + baseline snapshot

**Files:** none (setup)

- [ ] **Step 1: Create a work branch in `afterwords-app`**

```bash
cd ~/repos/afterwords-app
git checkout -b sprint5-app-redesign
```

- [ ] **Step 2: Record the live facts that must survive the reconciliation**

Run: `grep -noE "github.com/[^\" ]+|[0-9]+ voices|[0-9]+ profiles|[0-9]+ backends" docs/index.html`
Save the output. Every URL and count here must still be present (and unchanged in value) after the edit — the mockup must not overwrite them with stale figures.

---

### Task 1: Surface 3 — reconcile the app site

**Files:**
- Modify: `docs/index.html`
- Reference: `~/repos/afterwords/afterwords-redesign/Surface 3 - afterwords app.html`

- [ ] **Step 1: Enumerate the full delta (no edits yet)**

Open `docs/index.html` and the Surface 3 mockup side by side. Produce a written delta covering: hero text/treatment ("afterwords for Mac" vs "Afterwords"), section structure (live 3 sections vs mockup 2 — decide per-section: restyle, merge, or keep), card/feature styling, footer. For each factual element (counts, URLs, download link), mark **"live value wins"**.

- [ ] **Step 2: Apply the reconciliation**

Edit `docs/index.html` to match the mockup's visual treatment and section structure, while preserving every live fact recorded in Task 0 Step 2. Do not rename the palette tokens (C-5). Resolve the hero wording deliberately: keep "afterwords for Mac" if it carries product meaning the bare "Afterwords" loses — flag in the commit message which you chose and why.

- [ ] **Step 3: Verify live facts survived**

Run: `grep -noE "github.com/[^\" ]+|[0-9]+ voices|[0-9]+ profiles|[0-9]+ backends" docs/index.html`
Expected: every URL/count from Task 0 Step 2 is still present with the same value. No stale `296`/`100` figures introduced.

- [ ] **Step 4: HTML sanity check**

Run: `grep -c "<section" docs/index.html && grep -c "</section>" docs/index.html`
Expected: opening and closing `<section>` counts match (no broken markup from the restructure).

- [ ] **Step 5: Visual gate**

Open `docs/index.html` in a browser (it's static — `open docs/index.html`). Compare to the Surface 3 mockup. Acceptance: layout/sections/hero match the mockup's visual intent; all download/repo links resolve to the correct live URLs; no data drift.

- [ ] **Step 6: Commit**

```bash
cd ~/repos/afterwords-app
git add docs/index.html
git commit -m "feat(site): reconcile GitHub Pages site to Surface 3 (live URLs/counts preserved)"
```

---

### Task 2: Part B final verification

**Files:** none (verification)

- [ ] **Step 1: Link check**

Run: `grep -oE "https?://[^\" )]+" docs/index.html | sort -u`
Manually confirm each link is a real, current target (releases page, repo, any docs/cloud cross-links point at live URLs, not the mockup's placeholders).

- [ ] **Step 2: Push and report**

```bash
cd ~/repos/afterwords-app
git push -u origin sprint5-app-redesign
```
Report the visual-gate result and link check. Merge/Pages-publish is a separate user call.

---

## Self-review notes

- **Spec coverage:** B1 (Surface 3) → Task 1; verification → Task 2; C-5 explicitly no-action in Task 1 Step 2.
- **No build/test gates:** the app site is static HTML with no test suite or build; the honest gates are markup sanity (`<section>` balance), live-fact preservation (grep diff vs Task 0 snapshot), and manual visual check. Stated explicitly rather than forcing a TDD shape that doesn't exist here.
- **Sequencing:** Part B runs after Part A (cloud) is landed and verified, per the spec.
